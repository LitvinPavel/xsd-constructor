# 📋 Контекст проекта XSD Constructor

## 🎯 Назначение проекта
Веб-приложение для визуального конструирования XML документов на основе XSD схем.

## 🚀 Технологический стек
- Vue.js 3 (Composition API) + TypeScript
- Tailwind CSS 4.1.17
- DevExtreme UI компоненты
- Vite сборщик
- fast-xml-parser, xmlbuilder для XML

## 📁 Ключевые компоненты

### XSDForm.vue
Главный компонент формы:
- Загрузка XSD файлов (DxFileUploader)
- Отображение корневых элементов
- Генерация XML

### XSDGroup.vue (РЕКУРСИВНЫЙ)
Основной рекурсивный компонент:
- Обработка вложенных XSD элементов
- Поддержка sequence/all/choice/complexContent
- Управление состоянием элементов
- Добавление/удаление Entity, Property, Relation, LogicalUnit

### useForm.ts (Composition API)
Бизнес-логика:
- Парсинг XSD → JSON
- Управление состоянием элементов
- Генерация XML

### Типы данных

Основные интерфейсы в src/types/schema.ts:
- XSDElement, XSDComplexType, XSDSchema
- ComplexTypeInstance для моковых данных

## 🧩 Особенности реализации

### Рекурсивная структура
```vue
<!-- XSDGroup рекурсивно вызывает себя -->
<XSDGroup
  v-for="item in element.complexType?.sequence"
  :element="item"
  :level="level + 1"
  @update-value="handleChildUpdate"
/>
